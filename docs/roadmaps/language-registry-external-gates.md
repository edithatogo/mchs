# Language Registry External Gates

Language registry work is locally prepared where possible. Publication is intentionally not claimed without external registry evidence.

| Registry | Package | Local state | External gate |
| --- | --- | --- | --- |
| PyPI | `nwau-py` | published and verified at `https://pypi.org/project/nwau-py/0.2.2/` | complete |
| official MCP Registry | `io.github.edithatogo/mchs` | published and verified as version `0.2.2`; Smithery submission is tracked separately from language-registry submissions | complete for official MCP registry; Docker/Smithery follow their own MCP gates |
| npm | `@edithatogo/mchs-wasm-binding` | published and verified at `https://www.npmjs.com/package/@edithatogo/mchs-wasm-binding/v/0.1.0` | complete |
| crates.io | `nwau-core` | published and verified at `https://crates.io/crates/nwau-core/0.1.0`; checksum `c755101f5e206a92892250f35a4474a7fcac1cebb6d4782a5b97f8f6aa243547` | complete; browser-created crates token revoked and stale GitHub secret deleted |
| NuGet | `Mchs.Bindings.DotNet` | NuGet flat-container exposes `0.1.0` after workflow push in run `26404217645` | complete |
| CRAN | `nwauR` | `R CMD build` passed; `R CMD check --no-manual` returned `Status: OK`; package-local CRAN-style `_R_CHECK_CRAN_INCOMING_REMOTE_=false R CMD check --as-cran --no-manual` returned `Status: OK`; live `R CMD check --as-cran --no-manual` returned `Status: 1 NOTE` with only the expected `New submission` note; submitted through the CRAN web upload workflow on 2026-06-12; confirmation email received in Outlook at 02:17 Australia/Sydney; user clicked the confirmation link and CRAN displayed that the package was uploaded successfully to the CRAN submission team | Next actions: record incoming/pretest evidence, respond to CRAN mail, then verify `https://cran.r-project.org/package=nwauR` exposes `0.1.0` |
| Julia General | `NationalWeightedActivityUnitWrapper` | General PR `https://github.com/JuliaRegistries/General/pull/156254` merged on `2026-05-28T15:34:44Z`; repo `https://github.com/edithatogo/NationalWeightedActivityUnitWrapper.jl`; trigger issue `https://github.com/edithatogo/NationalWeightedActivityUnitWrapper.jl/issues/1`; tag `v0.1.0`; commit `56ddec5ae29513e80717d4625f82c024a211c949`; PR head `bb63b2a81ec2ded2c5675f09fb6cd63128f10a07`; UUID `58dad789-f56a-4ab3-a66f-c15139bf9cbe`; checks successful; superseded `NwauCore` PR `https://github.com/JuliaRegistries/General/pull/156236` preserved as superseded evidence | complete; publication is verified by the merged General PR |
| Go proxy/pkg.go.dev | Go binding module | Go module proxy and pkg.go.dev expose `v0.1.0` | complete |
| Swift Package Index | `MCHSBind` | `swift build` passed; PackageList issue closed completed; GitHub release `v0.1.0` remains published; 2026-06-12 public SPI probe returned HTTP 403 Cloudflare challenge without visible `MCHSBind` or `0.1.0` version evidence | public SPI listing/version evidence still pending; track-specific checklist: `conductor/tracks/swift_package_index_submission_20260524/public_probe_checklist.md` |
| Maven Central | `io.github.edithatogo:mchs-jvm-bindings` | 2026-06-12 refresh: Maven Central metadata still returns HTTP 404; Gradle build and Central Portal readiness check pass locally; jar, sources jar, javadoc jar, required POM metadata, signatures, and checksum sidecars are generated; Sonatype Central namespace `io.github.edithatogo` is verified through `https://github.com/edithatogo/f7fztfn9vz`; Central Portal upload bundle `build/mchs-jvm-bindings-0.1.0-central-bundle.zip` has SHA-256 `d0024c9f97b6cc23081139948a6b22508b5a06e20f96b75dc9b07082d2e56f42`; Publisher API uploads succeeded for deployments `89d0d2a9-91c6-4994-9f8e-fdd34bb501d0`, `fccefc51-9ccb-4466-8f85-8a47bc16cf3c`, and `7ced6d47-59ee-40fb-9c9b-09b1aa9f8491`, but Central validation failed because it could not discover public key `BB03C82343A653EE44BD5CDA9DF6B142F065199E`; the temporary Central user token was revoked | Next actions: wait for supported keyserver propagation or publish the key through another supported path, create a fresh short-lived Central token, re-upload/release through Central Portal or Publisher API, then verify Maven metadata contains `0.1.0`; track-specific checklist: `conductor/tracks/jvm_maven_central_registry_submission_20260524/submission_checklist.md` |
| conda-forge | `nwau-py` | branch updated against upstream `main`; linter, check-skip, linux_64, osx_64, win_64, and aggregate staged-recipes checks pass on PR `https://github.com/conda-forge/staged-recipes/pull/33452` | wait for maintainer review, merge, and feedstock publication; track-specific checklist: `conductor/tracks/conda_forge_feedstock_submission_20260524/review_checklist.md` |
| Homebrew | `nwau-py` | personal tap published; audit, source install, and `brew test` pass with Click resource plus lazy CLI patch; 2026-05-26 Homebrew/core formula API/page still return HTTP 404 | optional Homebrew/core PR/review |
| Open VSX / Visual Studio Marketplace | `mchs-tools` | concrete VS Code helper commands and `.vsix` package exist; deterministic Open VSX probe now uses `Accept: application/json` and confirms HTTP 404 not listed; Visual Studio Marketplace Gallery query returns 0 extensions; Eclipse account `edithatogo` is now linked to GitHub `edithatogo` and Open VSX GitHub login succeeds; Access Tokens still says the Eclipse Foundation Open VSX Publisher Agreement is not recognized and Log in with Eclipse reaches an Eclipse password prompt; Marketplace publisher `edithatogo` exists with Owner role | Next actions: complete the Eclipse password login/agreement-recognition flow, create `OVSX_PAT`, run the checked-in `ovsx publish` command, verify Open VSX version `0.1.0`, create `VSCE_PAT` for the existing Marketplace publisher, run the checked-in `vsce publish` command, then verify Gallery version `0.1.0` |
| MATLAB File Exchange | `mchs-matlab-interop` | source upload bundle includes README, MIT license, adapter functions, examples, and metadata | Next actions: sign in to MathWorks File Exchange, create submission `mchs-matlab-interop`, upload `mchs-matlab-interop-0.1.0.zip`, copy metadata from `file-exchange-submission.json`, record review evidence, then verify public version `0.1.0`; track-specific checklist: `conductor/tracks/matlab_file_exchange_submission_20260524/submission_checklist.md` |
| SSC / Stata package distribution | `mchs-stata-interop` | ado/help/pkg review bundle includes README, MIT license, notes, and examples; SSC submission and maintainer-identity clarification were sent via Gmail on 2026-06-12; follow-up should use `d.a.mordaunt@gmail.com` as the reachable Gmail account; no inbound maintainer response has been captured in repo evidence yet | Next actions: wait for SSC maintainer response or capture the user-visible Gmail response from the correct mailbox, apply maintainer fixes if requested, then verify SSC installability; track-specific checklist: `conductor/tracks/stata_ssc_submission_20260524/submission_checklist.md` |
| vcpkg / ConanCenter | `nwau-c-abi` | `cargo package --allow-dirty --locked` passes; Conan `create` builds and packages locally; disposable vcpkg bootstrap installs the local overlay port for `nwau-c-abi:arm64-osx`; vcpkg PR `https://github.com/microsoft/vcpkg/pull/51965` closed unmerged because vcpkg does not currently support Rust library ports; ConanCenter PR `https://github.com/conan-io/conan-center-index/pull/30262` is open after portability fixes | vcpkg is upstream-policy deferred until Rust library ports are supported or the package design avoids a Rust build in vcpkg. ConanCenter remains active: complete CLA/recheck, wait for job scheduler/review, then verify merged package pages; track-specific checklist: `conductor/tracks/c_cpp_vcpkg_conan_submission_20260524/upstream_pr_checklist.md` |

Run `python scripts/language_registry_external_gate_report.py` from `microcosting_healthservices/` for the current gate list.

Use `python scripts/language_registry_external_gate_report.py --live` to query recorded submission URLs, such as the conda-forge staged-recipes PR, and deterministic public package probes without claiming publication.

Use `python scripts/language_registry_external_gate_report.py --promotion` to group tracks into completion candidates, publication follow-up items, partial publications, submitted-review items, and external blocks from the checked-in contract without network access.

Use `python scripts/language_registry_external_gate_report.py --promotion --live` to refresh those groups with live submission and public-registry probes. Live completion candidates require target-version evidence from the public registry response; a package-level `200` without the expected version remains `public_listing_version_unverified`.

Add `--output <path>` to any mode to persist the report as a release or CI artifact.

The PR CI workflow writes non-live JSON artifacts only, so pull requests remain deterministic. The scheduled/manual `.github/workflows/language-registry-live.yml` monitor runs the live report commands and uploads `language-registry-live` artifacts for registry drift review.

The Go row is intentionally split: the Go proxy submission URL verifies the tagged module, while the public package probe checks `https://pkg.go.dev/github.com/edithatogo/mchs/bindings/go`. Once pkg.go.dev exposes the target version, the Go track can be completed.
