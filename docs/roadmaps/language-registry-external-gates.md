# Language Registry External Gates

Language registry work is locally prepared where possible. Publication is intentionally not claimed without external registry evidence.

| Registry | Package | Local state | External gate |
| --- | --- | --- | --- |
| PyPI | `nwau-py` | published and verified at `https://pypi.org/project/nwau-py/0.2.2/` | complete |
| official MCP Registry | `io.github.edithatogo/mchs` | published and verified as version `0.2.2`; Smithery submission is tracked separately from language-registry submissions | complete for official MCP registry; Docker/Smithery follow their own MCP gates |
| npm | `@edithatogo/mchs-wasm-binding` | published and verified at `https://www.npmjs.com/package/@edithatogo/mchs-wasm-binding/v/0.1.0` | complete |
| crates.io | `nwau-core` | published and verified at `https://crates.io/crates/nwau-core/0.1.0`; checksum `c755101f5e206a92892250f35a4474a7fcac1cebb6d4782a5b97f8f6aa243547` | complete; browser-created crates token revoked and stale GitHub secret deleted |
| NuGet | `Mchs.Bindings.DotNet` | NuGet flat-container exposes `0.1.0` after workflow push in run `26404217645` | complete |
| CRAN | `nwauR` | `R CMD build` passed; `R CMD check --no-manual` returned `Status: OK`; package-local CRAN-style `_R_CHECK_CRAN_INCOMING_REMOTE_=false R CMD check --as-cran --no-manual` returned `Status: OK`; live `R CMD check --as-cran --no-manual` returned `Status: 1 NOTE` with only the expected `New submission` note; submitted through the CRAN web upload workflow on 2026-06-12; confirmation email received in Outlook at 02:17 Australia/Sydney; user clicked the confirmation link and CRAN displayed that the package was uploaded successfully to the CRAN submission team; 2026-06-25 public package page still returns HTTP 404, CRANDB is not positive publication evidence from this network, and `src/contrib/PACKAGES` has no `Package: nwauR` entry | Next actions: record incoming/pretest evidence, respond to CRAN mail, then verify `https://cran.r-project.org/package=nwauR` exposes `0.1.0` |
| Julia General | `NationalWeightedActivityUnitWrapper` | General PR `https://github.com/JuliaRegistries/General/pull/156254` merged on `2026-05-28T15:34:44Z`; repo `https://github.com/edithatogo/NationalWeightedActivityUnitWrapper.jl`; trigger issue `https://github.com/edithatogo/NationalWeightedActivityUnitWrapper.jl/issues/1`; tag `v0.1.0`; commit `56ddec5ae29513e80717d4625f82c024a211c949`; PR head `bb63b2a81ec2ded2c5675f09fb6cd63128f10a07`; UUID `58dad789-f56a-4ab3-a66f-c15139bf9cbe`; checks successful; superseded `NwauCore` PR `https://github.com/JuliaRegistries/General/pull/156236` preserved as superseded evidence | complete; publication is verified by the merged General PR |
| Go proxy/pkg.go.dev | Go binding module | Go module proxy and pkg.go.dev expose `v0.1.0` | complete |
| Swift Package Index | `MCHSBind` | `swift build` passed; PackageList issue closed completed; maintainer comment confirmed the original closure was premature and PackageList PR `https://github.com/SwiftPackageIndex/PackageList/pull/13999` added `https://github.com/edithatogo/mchs-swift.git`; PR `13999` merged at `2026-06-12T12:02:16Z`; GitHub release `v0.1.0` remains published; 2026-06-12 public SPI page probe returned HTTP 200 and exposed `MCHSBind`, `edithatogo/mchs-swift`, stable `v0.1.0`, the SPM manifest snippet, and the GitHub release link | deprecated and cancelled on 2026-07-03; historical publication evidence retained only; no further SPI publication, compatibility, or monitoring work is planned unless re-chartered |
| Maven Central | `io.github.edithatogo:mchs-jvm-bindings` | Published and verified on repo1.maven.org as version `0.1.0`; Sonatype Central namespace `io.github.edithatogo` is verified; metadata URL exposes latest/release/version `0.1.0`; public JAR SHA-256 is `2f499b78d06317fd9bf2e343542b74043f163f127cd32db4651098f6ac6af49e`; successful Publisher API deployment `5fb01ae9-2609-4284-9427-5830e08bcbb5`; temporary Central user tokens were revoked after use | complete |
| conda-forge | `nwau-py` | branch updated against upstream `main`; authenticated live evidence on 2026-06-25 shows PR `https://github.com/conda-forge/staged-recipes/pull/33452` still open at head `bffc5bf1a85389dc695adfd96c87bf2413f4db25`, `mergedAt=null`, `draft=False`, `mergeable=MERGEABLE`, `mergeStateStatus=BLOCKED`, no review decision or new actionable comments, and linter, conda-forge-linter, staged-recipes, build fast finish, linux_64, osx_64, win_64, build status, and check skip all passing; Anaconda and feedstock probes still return HTTP 404 | wait for maintainer review, merge, and feedstock publication; track-specific checklist: `conductor/tracks/conda_forge_feedstock_submission_20260524/review_checklist.md` |
| Homebrew | `nwau-py` | personal tap published; audit, source install, and `brew test` pass with Click resource plus lazy CLI patch; 2026-05-26 Homebrew/core formula API/page still return HTTP 404 | optional Homebrew/core PR/review |
| Open VSX / Visual Studio Marketplace | `mchs-tools` | Open VSX API `https://open-vsx.org/api/edithatogo/mchs-tools` returns namespace `edithatogo`, name `mchs-tools`, latest version `0.1.1`, and allVersions including `0.1.1`, `0.1.0`, and `latest`; Visual Studio Marketplace is published and public as `edithatogo.mchs-tools` version `0.1.1`; publisher ID `bd039266-4396-4e4c-8bb8-13364a4aab70`; extension ID `8cf2c772-2ead-4a18-8dde-c5069790380a`; public Marketplace VSIX SHA-256 is `1d20feaa22e66978d5259dfb7b83467ed803a776d3fcb101792f2f164a2807ad`; fresh Open VSX token used for the publish check was deleted afterwards; local Marketplace-sync artifact `integrations/vscode/mchs-tools-0.1.1.vsix` is preserved with SHA-256 `bfbeca13497f21489c532e58af3b1e10df9fe60ae5eab4c721e632baee9b5dd6` | deprecated and cancelled on 2026-07-03; historical publication evidence retained only; no further extension publishing or synchronization work is planned unless re-chartered |
| MATLAB File Exchange | `mchs-matlab-interop` | Published and verified at `https://www.mathworks.com/matlabcentral/fileexchange/184067-mchs-matlab-interop`; page exposes `MCHS MATLAB Interop`, version `0.1.0`, add-on id `184067`, add-on UUID `91133d3e-f475-413c-85bc-544188a60074`, requirements text, and tags `cli`, `csv`, `health economics`, `matlab`, `microcosting`, and `parquet`; original uploaded archive SHA-256 is `1156f506cda8ab797b5d07adebc35ecccb36bd9758cffaf011029c71c9d2515a`; corrected local replacement archive SHA-256 is `d78cc11a9ab23080b38604e21c5d21ba9c8801ae0cf6219888f1797834cf2336` | complete for public `0.1.0`; replacement draft `aaea44a8-4710-4e2c-a17d-a97aede040de` remains a manual/browser follow-up if the corrected ZIP must be re-uploaded publicly |
| SSC / Stata package distribution | `mchs-stata-interop` | Published and verified through Boston College SSC/RePEc public files: `http://fmwww.bc.edu/repec/bocode/m/mchs.pkg`, `http://fmwww.bc.edu/repec/bocode/m/mchs.ado`, and `http://fmwww.bc.edu/repec/bocode/m/mchs.sthlp`; public manifest lists the MCHS module, Dylan Mordaunt support email, `mchs.ado`, `mchs.sthlp`, and example files; ado/help files expose the `mchs` command identity; local archive version `0.1.0` remains repository evidence because SSC `.pkg` metadata does not expose semantic versions | complete |
| vcpkg / ConanCenter | `nwau-c-abi` | `cargo package --allow-dirty --locked` passes; Conan `create` builds and packages locally; disposable vcpkg bootstrap installs the local overlay port for `nwau-c-abi:arm64-osx`; vcpkg PR `https://github.com/microsoft/vcpkg/pull/51965` closed unmerged because vcpkg does not currently support Rust library ports; ConanCenter PR `https://github.com/conan-io/conan-center-index/pull/30262` remains open after portability fixes and CLA resolution; 2026-06-25 live probe showed `license/cla` successful, `Job scheduler` `ACTION_REQUIRED`, `reviewDecision=REVIEW_REQUIRED`, `mergedAt=null`, `draft=False`, `mergeable=MERGEABLE`, `mergeStateStatus=BLOCKED`, and no new actionable comments after the 2026-06-12 author follow-up | deprecated and cancelled on 2026-07-03; PRs and local packaging evidence retained only as historical evidence; no further upstream review, publication, or monitoring work is planned unless re-chartered |

Run `python scripts/language_registry_external_gate_report.py` from `microcosting_healthservices/` for the current gate list.

Use `python scripts/language_registry_external_gate_report.py --live` to query recorded submission URLs, such as the conda-forge staged-recipes PR, and deterministic public package probes without claiming publication.

Use `python scripts/language_registry_external_gate_report.py --promotion` to group tracks into completion candidates, publication follow-up items, partial publications, approval-gated follow-ups, submitted-review items, and external blocks from the checked-in contract without network access. The JSON output includes `report.live`, live-only `report.generated_at_utc`, `next_actions`, `promotion_counts`, and `promotion_groups`.

Use `python scripts/language_registry_external_gate_report.py --promotion --live` to refresh those groups with live submission and public-registry probes. Live completion candidates require target-version evidence from the public registry response; a package-level `200` without the expected version remains `public_listing_version_unverified`.

Add `--output <path>` to any mode to persist the report as a release or CI artifact.

The CRAN live public probe checks the CRAN package page, CRANDB, and `https://cran.r-project.org/src/contrib/PACKAGES`; it does not treat a single reachable endpoint as publication evidence unless the target version is visible.

The conda-forge live public probe checks both the Anaconda `conda-forge/nwau-py` package API and `https://github.com/conda-forge/nwau-py-feedstock`; a feedstock-only result is treated as version-unverified until the Anaconda package API exposes the target version.

The vcpkg / ConanCenter live public probe remains available for historical drift review only. The surface is deprecated and cancelled, so ConanCenter and vcpkg evidence must not be promoted to active publication work unless a new track re-charters the surface.

The Stata SSC live public probe checks the Boston College SSC/RePEc `mchs.pkg`, `mchs.ado`, and `mchs.sthlp` paths. It treats SSC as installable when the package manifest and ado/help files expose the `mchs` command identity; the local semantic archive version remains repository evidence because SSC `.pkg` metadata does not expose semantic versions.

The PR CI workflow writes non-live JSON artifacts only, so pull requests remain deterministic. The scheduled/manual `.github/workflows/language-registry-live.yml` monitor runs the live report commands with `GITHUB_TOKEN` available for GitHub API calls, uploads `language-registry-live` artifacts for registry drift review, and appends the Markdown report to the GitHub Actions job summary with a generated timestamp, promotion group counts, submission detail, public detail, promotion state, and next actions. For GitHub PR submissions, submission detail includes live mergeability fields when the API provides them.

The Go row is intentionally split: the Go proxy submission URL verifies the tagged module, while the public package probe checks `https://pkg.go.dev/github.com/edithatogo/mchs/bindings/go`. Once pkg.go.dev exposes the target version, the Go track can be completed.
